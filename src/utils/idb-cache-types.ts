// Type definitions helper for IndexedDB wrapper
// This is just a placeholder type file if needed, but since we are using
// standard DOM types (IDBDatabase, etc.) in cacheStorage.ts, we don't strictly need this.
// keeping it empty or minimal if not required.
export interface IDBPCache { }
